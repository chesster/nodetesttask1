extends layout

block content
  h1= "Google Query"
  form(action="/", method="post")
    label Query:
    input(type="text", name="query", size=50, value=query)
    input(type="submit", value="Submit")
  if result
    ul
      each resultItem in result
        li
          h3=resultItem.header
          p=resultItem.description
